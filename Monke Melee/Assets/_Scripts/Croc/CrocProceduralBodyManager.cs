using UnityEngine;

public class CrocProceduralBodyManager : MonoBehaviour
{
    // Get all the bones in the spine
    // Set up IK chain spine

    // Raycast to the local -y direction from each ik target
    // rotate bone ik target to match normal of hit
}
